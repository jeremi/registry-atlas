[package]
name = "system-capability-discovery"
version = "0.1.0"
edition.workspace = true
license.workspace = true
readme = "README.md"

[dependencies]
semantic-asset-discovery-core = { path = "../semantic-asset-discovery-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
